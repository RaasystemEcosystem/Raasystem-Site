name: Deploy to GitHub Pages and AWS S3

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: Install dependencies
      run: npm install

    - name: Build site
      run: npm run build

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist  # or ./build if you're using React

    - name: Upload to AWS S3
      uses: jakejarvis/s3-sync-action@master
      with:
        args: --delete
      env:
        AWS_S3_BUCKET: raasystem.net  # Replace with your bucket name
        AWS_ACCESS_KEY_ID: ${{ YOUR_ACTUAL_ACCESS_KEY }}
        AWS_SECRET_ACCESS_KEY: ${{ YOUR_ACTUAL_SECRET_KEY }}
        AWS_REGION: us-west-2  # Replace with your region
        SOURCE_DIR: ./dist  # or ./build if React/Vite
 